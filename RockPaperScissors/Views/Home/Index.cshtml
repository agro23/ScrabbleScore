@{
  Layout = "_Layout";
}
    <div class="container well">
      <h1>Rock Paper Scissors</h1>
      <div class="row">
        <form class="form" action="/" method="post">
          <input type="text" name="PlayerOne" class="col-xs-3" id="player-one" placeholder="Player One's Choice">
          <input type="text" name="PlayerTwo" class="col-xs-3" id="player-two" placeholder="Player Two's Choice">
          <button type="submit" class="btn btn-success" name="button">PLAY!</button>
        </form>
      </div>
      <div class="row">
        @if(@Model["msg"].Length > 0)
        {
          <h3>@Model["msg"]</h3>
        }
      </div>
    </div>
    <div class="container" id="results">
      @if(@Model["msg"].Length > 0)
      {
        <h3>@Model["msg"]</h3>
      }
      <!-- <h3>@Model["msg"]</h3> -->
      <!-- <h2>RESULTS OF THE GAME</h2>
    </div> -->
